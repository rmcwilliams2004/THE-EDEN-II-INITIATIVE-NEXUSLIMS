import React from 'react';
import { KioskSimulatorView } from './KioskSimulatorView';

export const KioskDashboard: React.FC = () => {
  return <KioskSimulatorView />;
};

export default KioskDashboard;
